/**
 * @addtogroup grp_plrutils
 * @about 
 * Placeholder for functions written in PL/R
 * @par Related Topics
 * File plr_sample.sql_in, source file for all overloaded forms of the SQL functions
 *
 */

CREATE OR REPLACE FUNCTION DSTOOLS_SCHEMA.plr_cor(x float8[], y float8[])
    RETURNS float8
AS
$$
    return (cor(x,y));
$$ language plr;
