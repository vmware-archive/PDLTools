Pkgname: dstools
Architecture: @CPACK_RPM_PACKAGE_ARCHITECTURE@
Version: @DSTOOLS_GPPKG_VERSION@
OS: rhel5
GPDBVersion: @GPDB_VERSION@
Description: DSTools is an open source library to supplement the capabilities of MADlib, for in-database ML algorithms.
PostInstall:
- Master: "echo 'Please run the following command to deploy DSTools';
           echo 'usage:  dspack install [-s schema_name]  -c user@host:port/database';
           echo 'Example:';
           echo '       $ $GPHOME/dstools/bin/dspack install -s dstools -c gpadmin@mdw:5432/testdb';
           echo '       This will install DSTools objects into a Greenplum database named \"testdb\"';
           echo '       running on server \"mdw\" on port 5432. Installer will try to login as \"gpadmin\"';
           echo '       and will prompt for password. The target schema will be \"dstools\".';
           echo '       To upgrade to a new version of DSTools, use option \"-upgrade\",';
           echo '       instead of \"-install\" ';
           echo 'For additional options run:';
           echo '$ dspack --help';
           echo 'Release notes and additional documentation can be found at http://dstools.net';"
