Pkgname: dstools
Architecture: @CPACK_RPM_PACKAGE_ARCHITECTURE@
Version: @DSTOOLS_GPPKG_VERSION@
OS: rhel5
GPDBVersion: @GPDB_VERSION@
Description: DS Tools is a library of tools for Data Scientists.
PostInstall:
- Master: "echo 'Please run the following command to deploy DS Tools';
           echo 'usage:  dspack install [-s schema_name] [-S sugar_schema_name]';
           echo '                       -c user@host:port/database';
           echo 'Example:';
           echo '   $ $GPHOME/dstools/bin/dspack install -s dstools -c gpadmin@mdw:5432/testdb';
           echo '   This will install DSTools objects into a Greenplum database named \"testdb\"';
           echo '   running on server \"mdw\" on port 5432. Installer will try to login as';
           echo '   \"gpadmin\" and will prompt for password. The target schema for the main';
           echo '   library will be \"dstools\" (specified here explicitly) and the target';
           echo '   schema for the SUgAR library, packaged with it, will be \"sugarlib\"';
           echo '   (specified here implicitly). In both cases, these schema choices are';
           echo '   the default options.';
           echo '';
           echo 'To uninstall the package, use \"gppkg -r\".';
           echo 'For additional options run:';
           echo '$ dspack --help';
           echo 'Release notes and additional documentation can be found at';
           echo 'https://sites.google.com/a/gopivotal.com/global-data-science/ds-tools,';
           echo 'accessible through the Pivotal VPN.';"
